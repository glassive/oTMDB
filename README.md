```shell
cd tools
py -m venv .venv; .venv\scripts\activate; pip install -r .\requirements.txt
```

todo: refer to ikin's repo for tournament data
consider db exporting later on for convenience (mysql or sqlite)
